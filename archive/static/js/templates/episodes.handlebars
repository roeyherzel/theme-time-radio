
{{#each episode}}
  <div class="episode_card col-md-3">
    <a href="{{view_url}}">
      <div class="title">{{id}}. {{title}}</div>
      <img class="img-rounded" src={{image}}>
    </a>
  </div>
{{/each}}
