{% extends "layout.html.jinja" %}

{% block page_content %}
  <header class="clearfix">
    <img src="{{ episode.image }}" alt="Photo of {{ episode.title }}">
    <p class="episode-path">Season <span id="seasonId"> {{ episode.season }}</span> - Episode <span id="episodeId">{{ episode.id }}</span></p>
    <h1>{{ episode.title }}</h1>
    <p class="release-date">{{ episode.aired }}</p>
  </header>

  <section id="episodePlayer">
    <ul>
      <li><button id="episodePrevButton" type="button"></button></li>
      <li><button id="episodePlayButton" type="button"></button></li>
      <li><button id="episodeNextButton" type="button"></button></li>
    </ul>

    <div id="episodeProgressBar"></div>

    <div class="clearfix">
      <div id="episodeTimeProgress">
        <span id="episodePlayerCurrTime">0:00</span>
        <span> / </span>
        <span id="episodePlayerDurTime">0:00</span>
      </div>
      <button id="episodeVolButton" type="button"></button>
    </div>
  </section>

  <main>
    <ul class="tabs">
      <li class="tab"><a href="#">Intro</a></li>
      <li class="tab-content">
        <section id="intro">
          <p>{{ episode.description }}...</p>
        </section>
      </li>

      <li class="tab"><a href="#">Tracklist</a></li>
      <li class="tab-content">
        <section id="tracklist"></section> <!-- tracklist placeholder -->
      </li>

      <li class="tab"><a href="#">Artists</a></li>
      <li class="tab-content">
        <section id="artists"></section> <!-- artists placeholder -->
      </li>
    </ul>
  </main>
{% endblock %}
