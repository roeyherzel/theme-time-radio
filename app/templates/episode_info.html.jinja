{% extends "layout.html.jinja" %}

{% block page_content %}
  <header>
    <img src="{{ episode.image }}" alt="Photo of {{ episode.title }}">
    <p class="episode-path">Season <span id="seasonId"> {{ episode.season }}</span> - Episode <span id="episodeId">{{ episode.id }}</span></p>
    <h1>{{ episode.title }}</h1>
    <p class="release-date">{{ episode.aired }}</p>
  </header>

  <section id="episode_player">
    <div id="episode_player_controls">
      <button id="episode_prev_button" type="button"></button>
      <button id="episode_play_button" type="button"></button>
      <button id="episode_next_button" type="button"></button>
    </div>

    <div id="episode_progress_bar"></div>

    <div id="episode_progress_time">
      <span id="episode_current">0:00</span>
      <span> / </span>
      <span id="episode_duration">0:00</span>
    </div>
    <!-- <button id="episode_vol_button" type="button"></button> -->

  </section>

  <main>
    <ul class="tabs">
      <li class="tab"><a href="#">Intro</a></li>
      <li class="tab-content">
        <section id="intro">
          <p>{{ episode.description }}...</p>
        </section>
      </li>

      <li class="tab"><a href="#">Tracklist</a></li>
      <li class="tab-content">
        <section id="tracklist"></section> <!-- tracklist placeholder -->
      </li>

      <li class="tab"><a href="#">Artists</a></li>
      <li class="tab-content">
        <section id="artists"></section> <!-- artists placeholder -->
      </li>
    </ul>
  </main>
{% endblock %}
